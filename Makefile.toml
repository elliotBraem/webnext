[env]
CARGO_MAKE_EXTEND_WORKSPACE_MAKEFILE = true

[tasks.format]
install_crate = "rustfmt"
command = "cargo"
args = ["fmt", "--", "--emit=files"]

[tasks.clean]
command = "cargo"
args = ["clean"]

[tasks.libs-build]
command = "cargo"
args = ["build", "--target", "wasm32-unknown-unknown", "--release", "--lib"]
dependencies = ["clean"]

[tasks.desktop-space-dev-build]
workspace = false
install_script = "cargo install --git https://github.com/DioxusLabs/cli"
cwd = "./spaces/desktop/"
command = "dioxus"
args = ["build", "--platform", "web"]

[tasks.desktop-space-prod-build]
workspace = false
install_script = "cargo install --git https://github.com/DioxusLabs/cli"
cwd = "./spaces/desktop/"
command = "dioxus"
args = ["build", "--platform", "web", "--release"]

[tasks.desktop-space-dev-serve]
workspace = false
install_script = "cargo install --git https://github.com/DioxusLabs/cli"
cwd = "./spaces/desktop/"
command = "dioxus"
args = ["serve", "--platform", "web", "--hot-reload", "--port", "1337"]

[tasks.promo-space-dev-build]
workspace = false
install_script = "cargo install --git https://github.com/DioxusLabs/cli"
cwd = "./spaces/promo/"
command = "dioxus"
args = ["build", "--platform", "web"]

[tasks.promo-space-prod-build]
workspace = false
install_script = "cargo install --git https://github.com/DioxusLabs/cli"
cwd = "./spaces/promo/"
command = "dioxus"
args = ["build", "--platform", "web", "--release"]

[tasks.promo-space-dev-serve]
workspace = false
install_script = "cargo install --git https://github.com/DioxusLabs/cli"
cwd = "./spaces/promo/"
command = "dioxus"
args = ["serve", "--platform", "web", "--hot-reload", "--port", "1337"]

[tasks.dev]
workspace = false
dependencies = ["desktop-space-dev-serve"]
